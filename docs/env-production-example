# Configuration environnement production MedimexResolv
# MODIFIEZ TOUS LES MOTS DE PASSE AVANT DÉPLOIEMENT !

# Application
NODE_ENV=production
PORT=3000
FRONTEND_URL=https://votre-domaine.com

# Base de données MariaDB
DB_HOST=localhost
DB_USER=medimex
DB_PASSWORD=CHANGEZ_MOI_MOT_PASSE_SECURISE_123!@#
DB_NAME=medimex_resolv
DB_PORT=3306
DB_ROOT_PASSWORD=CHANGEZ_MOI_ROOT_SECURISE_456!@#

# Sécurité JWT - GÉNÉREZ UNE CLÉ UNIQUE !
JWT_SECRET=CHANGEZ_MOI_CLE_SECRETE_TRES_LONGUE_ET_COMPLEXE_789!@#
JWT_EXPIRE=8h

# Redis (optionnel mais recommandé)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=CHANGEZ_MOI_REDIS_SECURISE_101112!@#

# Upload et stockage
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
BACKUP_DIR=./backups

# Logs
LOG_LEVEL=info
LOG_DIR=./logs

# Email SMTP (configurez selon votre fournisseur)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=true
EMAIL_USER=votre-email@entreprise.com
EMAIL_PASS=votre-mot-passe-application
EMAIL_FROM=noreply@votre-domaine.com

# Sécurité
BCRYPT_ROUNDS=12
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000
SESSION_TIMEOUT_HOURS=8
MAX_LOGIN_ATTEMPTS=3
LOGIN_LOCKOUT_MINUTES=15

# Monitoring (optionnel)
GRAFANA_PASSWORD=CHANGEZ_MOI_GRAFANA_ADMIN_131415!@#

# Sauvegardes
BACKUP_RETENTION_DAYS=30
BACKUP_SCHEDULE_HOUR=2

# SSL/HTTPS (en production)
SSL_CERT_PATH=/etc/ssl/certs/medimex.crt
SSL_KEY_PATH=/etc/ssl/private/medimex.key

# Configuration MariaDB optimisée
MARIADB_INNODB_BUFFER_POOL_SIZE=256M
MARIADB_MAX_CONNECTIONS=200
MARIADB_QUERY_CACHE_SIZE=64M

# Variables Docker
COMPOSE_PROJECT_NAME=medimex-resolv
COMPOSE_FILE=docker-compose.production.yml